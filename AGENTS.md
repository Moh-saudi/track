# AGENTS.md — منظومة تتبع القضايا الواردة إلى اللجنة العليا للمسئولية الطبية

هذا الملف مرجع كامل لأي Agent يعمل على هذا المشروع. اقرأه بالكامل قبل تنفيذ أي مهمة، والتزم بكل
القرارات المذكورة فيه دون تغييرها إلا إذا طلب المستخدم ذلك صراحة.

## ١. نظرة عامة على المشروع

نظام لتتبع القضايا والشكاوى والمحاضر الواردة من النيابة العامة والمواطنين إلى اللجنة العليا
للمسئولية الطبية في مصر، بدءًا من التسجيل وحتى القرار النهائي والتسوية المالية لأعضاء اللجان.
الأساس التنظيمي: القرار الرسمي الصادر بتاريخ 5 أبريل 2021 (مادة رقم 1) بتشكيل 16 لجنة فرعية.

الجمهور المستهدف نهائيًا: جهة حكومية (وزارة الصحة والسكان المصرية). لذلك:
- **لا اعتماد على أي مزود سحابي خارجي** (تم استبعاد Supabase عمدًا) — كل شيء self-hosted من اليوم الأول لتسهيل الترحيل لسيرفرات الوزارة لاحقًا دون إعادة بناء أي طبقة.
- التعامل مع بيانات حساسة (رقم قومي، بيانات طبية/قانونية) يستوجب أعلى درجات الحرص الأمني.

## ٢. سير العمل (لا تُغيَّر مراحله دون تأكيد صريح من المستخدم)

1. **موظف التسجيل (REGISTRATION_CLERK)**: يستلم الشكوى/القضية/المحضر ويسجّل بياناتها الأولية فقط.
   **لا يقوم بالتوجيه** — هذه ليست وظيفته.
2. **موظف المتابعة والتوجيه (ROUTING_OFFICER)** — دور مستقل بالكامل عن موظف التسجيل:
   - يوجّه كل قضية مسجَّلة حديثًا إلى اللجنة الفرعية المختصة.
   - يتابع باستمرار عمل كل اللجان الفرعية النشطة: هل كل قضية ما زالت داخل الإطار الزمني المتوقع
     (بين تاريخ الاستلام وتاريخ الانعقاد المتوقع/تاريخ إصدار التقرير) أم تجاوزته؟ يحتاج شاشة متابعة
     (Tracking Dashboard) تُظهر كل قضية موجّهة مع مؤشر: "في الموعد" / "متأخرة" بناءً على مقارنة
     التواريخ الفعلية بمدة زمنية معيارية (SLA) تُضبط لاحقًا من الإدارة.
3. **اللجنة الفرعية المختصة**: تستلم القضية (تاريخ الاستلام)، تعقد اجتماعًا (تاريخ الانعقاد)، تكتب
   **توصيف الخطأ**، ثم تُصدر تقريرًا (تاريخ إصدار التقرير — يجب أن يكون بعد تاريخ الانعقاد) والإجراء المتخذ.
4. **اللجنة العليا**: تراجع تفاصيل القضية وتقرير اللجنة الفرعية والمرفقات، تسجّل تاريخ انعقادها،
   وتصدر أحد ثلاثة قرارات: **اعتماد** قرار اللجنة الفرعية / **إحالة لإعادة الدراسة** للجنة فرعية أخرى / **قرار مغاير**.
5. **الإدارة المالية**: تحدد استحقاق كل عضو لجنة بناءً على القرار النهائي، وتحدّث حالة الصرف
   (لم يُصرف / تحت التسوية / تم الصرف).

> ملاحظة تحديث: كانت خطوة التوجيه سابقًا من مهام موظف التسجيل — تم فصلها في دور مستقل (موظف
> المتابعة والتوجيه) بناءً على طلب المستخدم. أي كود أو واجهة قديمة تفترض أن موظف التسجيل يوجّه
> القضايا يجب تصحيحها.

مخططا سير العمل وتدفق البيانات بين الحسابات والجداول في مستند الـ PRD (القسم 5) قديمان بالنسبة
لهذا التقسيم الجديد للأدوار — حدّثهما عند أي عمل على المخططات لاحقًا.

## ٣. الأدوار وقواعد الرؤية (RBAC) — إلزامية، لا تُخفَّف أبدًا

| الدور | يرى | يفعل |
|---|---|---|
| REGISTRATION_CLERK (موظف التسجيل) | القضايا التي سجّلها | إنشاء قضية فقط — **لا يوجّهها** |
| ROUTING_OFFICER (موظف المتابعة والتوجيه) | كل القضايا غير الموجَّهة + شاشة متابعة زمنية لكل القضايا الموجَّهة | توجيه القضية للجنة فرعية، متابعة التزام كل لجنة بالإطار الزمني |
| SUBCOMMITTEE_MEMBER (عضو لجنة فرعية) | **قضايا لجنته فقط** | تسجيل الاستلام/الانعقاد/توصيف الخطأ/التقرير/الإجراء، رفع مرفقات |
| SUPREME_COMMITTEE (عضو اللجنة العليا) | كل القضايا | إصدار القرار النهائي فقط |
| FINANCE (الإدارة المالية) | الجدول المالي وقرارات اللجنة العليا | تحديث حالة الصرف |
| ADMIN (مدير النظام) | كل شيء | إدارة المستخدمين، اللجان الفرعية وتخصصاتها، الأعضاء، الصلاحيات |

قاعدة صارمة: عضو اللجنة الفرعية لا يجب أن يرى أو يعدّل أي قضية غير موجهة للجنته تحت أي ظرف — هذا
مطبَّق حاليًا في `src/lib/rbac.ts` و `src/middleware.ts` و فلاتر `where` في كل API route؛ حافظ على
هذا النمط في أي كود جديد، وأضِف دور `ROUTING_OFFICER` بنفس الصرامة (صلاحية التوجيه تُنقل إليه بالكامل
وتُزال من `REGISTRATION_CLERK`).

## ٤. نموذج البيانات (Prisma) — لا تُضِف/تحذف حقولًا دون داعٍ واضح

راجع `prisma/schema.prisma` كمرجع نهائي. أهم الحقول المتفق عليها بالاسم:

- **Case**: `registrationType` (COMPLAINT شكوى / CASE قضية / REPORT محضر)، `caseNumber`،
  `reportYear` (إلزامي فقط عند REPORT — يُعرض كـ "الرقم لسنة السنة")، `description` (وصف السجل)،
  `prosecution`، `governorate`، `complainantName`، `status`، `routedById` (المستخدم من دور
  ROUTING_OFFICER الذي وجّه القضية)، `routedAt` (تاريخ التوجيه — أساس حساب الالتزام الزمني لاحقًا).
- **CaseAction**: `receivedDate`، `meetingDate`، `faultDescription` (توصيف الخطأ)، `reportDate`
  (يجب التحقق أنه بعد `meetingDate`)، `reportText`، `actionTaken`.
- **SupremeDecision**: `meetingDate` (تاريخ انعقاد اللجنة العليا)، `decisionType`
  (APPROVE/REFER_BACK/DIFFERENT)، `decisionDetails`، `referredSubCommitteeId` (إلزامي فقط عند REFER_BACK).
- **Payment**: `entitled`، `status` (NOT_PAID/UNDER_SETTLEMENT/PAID)، `amount`.
- **SubCommittee**: `name`، `active`. **لم تعُد** تحمل حقل تخصص نصي واحد — التخصصات أصبحت كيانًا
  منفصلاً (`Specialty`) بعلاقة **واحد-إلى-متعدد** مع اللجنة الفرعية: كل لجنة فرعية يمكن أن تحمل عدد
  تخصصات مختلف (لجنة قد تكون بلا تخصصات فرعية بعد، وأخرى بعشرة). الأدمن فقط يملك صلاحية إضافة لجنة
  فرعية جديدة، وإضافة/تعديل/حذف تخصصات تحت أي لجنة (`ADMIN` فقط عبر واجهة `/dashboard/admin`).
- **Specialty**: `name`، `subCommitteeId` (مرتبط بلجنة فرعية واحدة). عند توجيه قضية، يختار
  ROUTING_OFFICER اللجنة الفرعية ثم (اختياريًا) التخصص الدقيق داخلها إن وُجد.
- **Attachment**: مرتبط بـ Case، يخزَّن حاليًا محليًا على القرص (`/uploads`)، مرئي لكل الأدوار
  المصرح لها بالوصول لتلك القضية (تراكميًا عبر مراحل القضية).
- **CommitteeMember**: `nationalIdCipher` — الرقم القومي **يُخزَّن مشفّرًا فقط** عبر `src/lib/crypto.ts`، لا تخزّنه أبدًا كنص صريح.
- **AuditLog**: أي عملية إنشاء/تعديل/تغيير حالة على أي كيان رئيسي (Case, CaseAction,
  SupremeDecision, Payment, Attachment) يجب أن تُسجَّل عبر `writeAuditLog()` — هذا التزام ثابت وليس اختياريًا.

## ٥. الحزمة التقنية (ثابتة — لا تستبدلها)

- **Next.js 14 (App Router) + TypeScript** — الواجهة الأمامية والخلفية معًا.
- **PostgreSQL عبر Prisma ORM** — قاعدة بيانات قياسية قابلة للترحيل، لا NoSQL ولا BaaS.
- **المصادقة: NextAuth (Credentials Provider) + bcrypt** فقط — **ممنوع** استخدام Supabase Auth أو أي
  مزود OAuth سحابي خارجي كبديل أساسي؛ الهدف صراحة تفادي إعادة بناء طبقة الدخول عند الترحيل لسيرفرات
  الوزارة.
- **تخزين الملفات**: القرص المحلي حاليًا (`/uploads`)، مصمَّم ليكون قابلاً للاستبدال لاحقًا بتخزين
  على سيرفر الوزارة دون تغيير هيكلي كبير — لا تربطه بخدمة تخزين سحابية خارجية (S3، إلخ) كحل دائم.
- **zod** للتحقق من مدخلات كل API route.

## ٦. هوية التصميم (Design System) — إصدار مُعاد تصميمه بالكامل، التزم به حرفيًا

> هذا القسم يُلغي ويستبدل أي هوية تصميم سابقة كانت مطبَّقة على المشروع. أعِد تصميم كل الشاشات
> الحالية (تسجيل الدخول، كل لوحات التحكم، الجداول، الأزرار) لتطابق هذا القسم — لا تكتفِ بإضافته على
> الشاشات الجديدة فقط.

- **اللغة والاتجاه**: عربي بالكامل، RTL (`dir="rtl"`, `lang="ar"`). الخطوط: **Cairo** للعناوين
  (وزن 600–700) و **Tajawal** للنصوص العادية (وزن 400–500).
  **مشكلة معروفة يجب إصلاحها أولاً**: النسخة الحالية المنشورة (trackcd.vercel.app) خطوطها تبدو
  "سيئة" وبلا تناسق في الأحجام — على الأغلب لأن Cairo/Tajawal لا تُحمَّل فعليًا عبر الشبكة وتتساقط
  (fallback) لخط النظام الافتراضي، أو لأن الأحجام مكتوبة بقيم عشوائية بدل مقياس ثابت. الإصلاح:
  1. حمّل الخطوط عبر `next/font/google` حصريًا (وليس رابط `<link>` يدوي قد يُحجب أو يتأخر)، مثال:
     ```ts
     import { Cairo, Tajawal } from "next/font/google";
     const cairo = Cairo({ subsets: ["arabic"], weight: ["600", "700"], variable: "--font-heading" });
     const tajawal = Tajawal({ subsets: ["arabic"], weight: ["400", "500"], variable: "--font-body" });
     ```
     واربطهما بـ `className` على `<html>` أو `<body>` في `layout.tsx`، وفعّلهما في `tailwind.config`
     ضمن `fontFamily.heading` و `fontFamily.body` — لا نص واحد في الواجهة يُترك بخط المتصفح الافتراضي.
  2. لا تستخدم أي حجم خط "حر" (`text-[13px]`, `style={{fontSize: 15}}`, إلخ) — كل نص يجب أن يستخدم
     أحد أحجام المقياس الثابت التالي فقط:

  | الاستخدام | الحجم | الوزن | مثال Tailwind |
  |---|---|---|---|
  | عنوان صفحة رئيسي (H1) | 28px / 1.75rem | Cairo 700 | `text-3xl font-bold font-heading` |
  | عنوان قسم (H2) | 22px / 1.375rem | Cairo 600 | `text-xl font-semibold font-heading` |
  | عنوان بطاقة/عنصر فرعي (H3) | 18px / 1.125rem | Cairo 600 | `text-lg font-semibold font-heading` |
  | نص أساسي (body) | 15px / 0.9375rem | Tajawal 400 | `text-[15px] font-body` (استثناء وحيد مسموح لأنه القيمة المرجعية للـ body) |
  | نص ثانوي/وصفي | 13px / 0.8125rem | Tajawal 400، لون `slate-500` | `text-sm text-slate-500 font-body` |
  | رقم كبير داخل بطاقة KPI | 32px / 2rem | Cairo 700 | `text-4xl font-bold font-heading` |
  | تسمية/Label صغيرة (badge, caption) | 12px / 0.75rem | Tajawal 500 | `text-xs font-medium font-body` |

  ارتفاع السطر (line-height): `leading-snug` للعناوين، `leading-relaxed` للنصوص الطويلة — النصوص
  العربية تحتاج تباعد أسطر أكبر من اللاتينية بسبب التشكيل البصري للحروف.
- **Tailwind CSS مسموح ومفضَّل** لهذا الإصدار (تراجع صريح عن قيد سابق كان يمنعه) — استخدمه بدلاً من
  الـ inline styles في كل شاشة تُعاد كتابتها.

### مقاييس تناسق إضافية (لحل مشكلة "عدم التناسب" في النسخة الحالية)

- **مقياس تباعد ثابت (Spacing Scale) على أساس 4px**: استخدم فقط `p-1/p-2/p-3/p-4/p-6/p-8` وما
  يعادلها من `gap-*` و `space-y-*` — ممنوع قيم تباعد حرة (`p-[13px]`). البطاقات دائمًا `p-6`، الفراغ
  بين البطاقات دائمًا `gap-4` أو `gap-6`، لا تباعد عشوائي مختلف من بطاقة لأخرى.
- **ارتفاع موحّد للعناصر التفاعلية**: كل الأزرار وحقول الإدخال والقوائم المنسدلة بنفس الارتفاع
  `h-10` (40px) في نفس الصف — من العيوب الشائعة اختلاف ارتفاع الأزرار عن حقول البحث المجاورة لها.
  الأيقونات داخل البطاقات (زي الدائرة الحمراء/الخضراء في الصورة المرفقة) بحجم موحّد `w-10 h-10`.
  - **الأيقونات**: مكتبة واحدة فقط (`lucide-react`) بحجم موحّد `20px` أو `24px` — لا خليط من
    إيموجي (🚨⚠️) وأيقونات SVG في نفس الشاشة كما هو حاصل حاليًا في بطاقات الـ KPI.
- **التباين اللوني (Contrast)**: أي نص على خلفية ملوّنة يجب أن يحقق نسبة تباين WCAG AA (4.5:1
  كحد أدنى) — راجع بطاقات KPI الحمراء/الخضراء في اللقطة المرفقة، النص الأحمر الغامق على خلفية
  وردية فاتحة ضعيف التباين، استخدم `text-red-800` على `bg-red-50` بدل الدرجات المتوسطة.
- **حالة القيم صفر**: البطاقة لما تكون قيمتها 0 يجب أن تُعرض بلون محايد (`slate`) وليس بنفس لون
  التحذير (أحمر/برتقالي) — إظهار "0" بلون تحذيري كما في اللقطة الحالية يعطي انطباعًا خاطئًا بوجود مشكلة.
- **الاتساق بين الصفحات**: أي مكوّن (بطاقة KPI، جدول، badge) يُبنى مرة واحدة كمكوّن React قابل
  لإعادة الاستخدام في `src/components/ui/` ويُستدعى من كل الشاشات — ممنوع تكرار نفس الشكل بأنماط
  مختلفة قليلاً في كل صفحة، فهذا هو سبب شعور "عدم التناسق" بين الشاشات.


### لوحة الألوان (Tailwind-style tokens)

- **الأساسي (Primary/Brand)**: نفس زُرقة-مُخضرّة هادئة توحي بالثقة الطبية دون رسمية جامدة —
  `teal-600 #0D9488` للأزرار والروابط النشطة والعناصر التفاعلية، `teal-700 #0F766E` عند hover.
- **المحايد (Neutral)**: توسّع من `slate-50 #F8FAFC` (خلفية عامة) إلى `slate-900 #0F172A`
  (نص أساسي/الشريط الجانبي). لا تستخدم الأبيض/الأسود الصريحين.
- **ألوان مميّزة لكل دور** (Accent — تُستخدم كشارة/أيقونة صغيرة بجانب اسم الدور، ليست خلفية كاملة):
  - موظف التسجيل: `sky-600 #0284C7`
  - موظف المتابعة والتوجيه: `violet-600 #7C3AED`
  - اللجنة الفرعية: `amber-600 #D97706`
  - اللجنة العليا: `teal-600 #0D9488` (نفس اللون الأساسي — أهم دور قرارًا)
  - الإدارة المالية: `emerald-600 #059669`
  - مدير النظام: `slate-600 #475569`
- **شارات حالة القضية (Status badges — نص ملوّن على خلفية فاتحة من نفس العائلة، شكل Pill)**:
  - بانتظار التوجيه: `slate-100` / `slate-700`
  - قيد الدراسة الفرعية: `amber-100` / `amber-800`
  - بانتظار اعتماد اللجنة العليا: `sky-100` / `sky-800`
  - معتمدة: `emerald-100` / `emerald-800`
  - محالة لإعادة الدراسة: `rose-100` / `rose-800`
  - **متأخرة عن الإطار الزمني** (جديدة، لشاشة متابعة ROUTING_OFFICER): `red-100` / `red-800` + أيقونة تحذير

### تخطيط الصفحة (Layout) — الهوية الرسمية المعتمدة النهائية

- **الشريط العلوي (Top Bar)**:
  - "Official logo and institutional title belong in Top Bar."
  - **أقصى اليمين (RTL)**: الهوية المؤسسية الرسمية (`CabinetLogo.tsx` باستخدام `public/logo.svg` فقط دون بديل):
    - الشعار الرسمي لجمهورية مصر العربية (رئاسة مجلس الوزراء).
    - النصوص الرسمية المدمجة: "جمهورية مصر العربية" (خط صغير رسمي)، "رئاسة مجلس الوزراء" (وزن متوسط)، "اللجنة العليا للمسؤولية الطبية وسلامة المريض" (خط واضح رسمي دون تضخيم مبالغ فيه).
  - **المنتصف**: شريط البحث الموحد (Global Search) بحجم `h-10 rounded-lg` بنص إرشادي: "ابحث برقم السجل، اسم الشاكي، النيابة...".
  - **أقصى اليسار**: مؤشر حالة المنظومة المحايد، زر الإشعارات والتنبيهات، وقائمة المستخدم (`UserMenu.tsx`).
- **قائمة المستخدم (User Identity)**:
  - "User identity belongs in Top Bar, not Sidebar."
  - لا تظهر بطاقة المستخدم داخل الشريط الجانبي (Sidebar) نهائياً.
  - تظهر في أقصى يسار الـ Top Bar بصورة/حرف المستخدم، اسمه، دوره، وقائمة منسدلة (Dropdown) تحتوي على: الملف الشخصي، جهة العمل، تغيير كلمة المرور والأمان، وتسجيل الخروج.
- **الشريط الجانبي (Sidebar)**:
  - **خلفية فاتحة ونظيفة (Light Neutral)**: بلون `#F7FAF9` أو `slate-50`، مع حد جانبي أيسر خفيف (`border-l border-slate-200`).
  - **ممنوع تماماً** استخدام الكحلي الداكن أو `slate-900` كخلفية للشريط الجانبي، وممنوع التدرجات (Gradients).
  - **العرض**: بين `248px` و `264px` (`w-64` = 256px).
  - **المحتوى**: مخصص حصرياً للتنقل: تقسيم المجموعات (مثل "المهام والعمليات"، "الإدارة والتهيئة")، الأيقونات من `lucide-react` (بحجم 20px)، والنصوص.
  - **الحالة النشطة (Active state)**:
    - خلفية `teal-50`
    - نص `teal-800`
    - أيقونة `teal-700`
    - خط رأسي رفيع على يمين العنصر بسمك 3px ولون `teal-600` (`border-r-[3px] border-r-teal-600`).
  - **الحالة غير النشطة (Inactive)**: نص `slate-600` مع هوفر `slate-100` أو `teal-50` خفيف جداً.
  - **التجاوب (Responsive)**: يتحول الشريط الجانبي في الشاشات الصغيرة والموبايل إلى درج جانبي منزلق (Mobile Drawer) مع زر فتح القائمة (Hamburger menu) في الـ Top Bar وخلفية معتمة (Backdrop).
- **أعلى كل لوحة تحكم: صف بطاقات KPI موجزة** (3–4 بطاقات فقط):
  - خلفية بيضاء، `rounded-2xl`، إطار `border-slate-200`، ظل خفيف `shadow-sm`، padding سخي (`p-6`).
  - حاوية أيقونة بحجم `w-10 h-10` وأيقونة `lucide-react` بحجم 20px أو 24px.
  - الأرقام بحجم 32px (`text-[32px]` أو `text-4xl font-bold font-heading`).
  - التسمية بحجم 13px (`text-[13px] font-medium text-slate-600 font-body`).
  - **حالة القيم صفر**: تعرض بلون محايد (`slate`) وليس بلون تحذيري إلا إذا كانت القيمة تمثل مشكلة حقيقية (مثل قضايا متأخرة > 0).
- **عناوين الصفحات (Page Header)**:
  - مسار تصفح مصغر (Breadcrumb) مثل: "الرئيسية / قيد السجلات".
  - عنوان رئيسي H1 بحجم 28px (`text-3xl font-bold font-heading`).
  - وصف وظيفي مختصر يوضح طبيعة الشاشة، دون تكرار اسم الجهة الحكومية داخل كل صفحة.
- **الجداول**:
  - رأس الجدول بخلفية `slate-50` ونص `slate-600` صغير الحجم (`text-[11px] font-bold font-heading`).
  - صفوف بيضاء بحد سفلي `slate-100` وهوفر خفيف `slate-50/70`.
  - حالة السجل تعرض كـ Pill Badge بألوان العائلة المحددة.
  - قائمة إجراءات منسدلة مدمجة بزر `MoreHorizontal` لتفادي تكدس الأزرار.
  - التجاوب في الموبايل: تحويل صفوف الجدول إلى بطاقات قضايا ذكية (Case Cards) لتفادي التمرير الأفقي المرهق.
- **الأزرار وحقول الإدخال**:
  - ارتفاع موحد لكافة عناصر الإدخال والأزرار والقوائم المنسدلة: `h-10` (40px).
  - الأزرار: أساسي = خلفية `teal-600` نص أبيض `rounded-lg`؛ ثانوي = حدود `slate-300` نص `slate-700` بلا خلفية؛ تحذيري = `rose-600`.
- **الخلفية العامة** لكافة الصفحات: `slate-50` (#F8FAFC).

هذا القسم يلغي أي قيود قديمة للكحلي الداكن أو وجود بطاقة المستخدم داخل الـ Sidebar.

### تعليمات إلزامية إضافية — بسبب مخالفات مؤكدة في نسخة سابقة (لا تكرّرها)

فحص فعلي لريبو `github.com/Moh-saudi/track` كشف أن التعليمات أعلاه لم تُطبَّق فعليًا رغم وجودها في
نسخة سابقة من هذا الملف. التزم بالتالي حرفيًا لمنع تكرار نفس الأخطاء:

1. **احذف تمامًا** أي تعريف ألوان قديم باسم `gov` أو أي اسم آخر يحمل القيمة `#1F4E79` من
   `tailwind.config.ts` — لا تُبقِه معرَّفًا حتى لو غير مُستخدم، لأن وجوده يعني عودة استخدامه لاحقًا.
   ضع بدلاً منه ألوان `teal`/`slate` القياسية من Tailwind مباشرة (لا حاجة لتعريف مخصص أصلاً).
   ثم ابحث بـ `grep -rn "1F4E79" src` وتأكد أن النتيجة صفر قبل اعتبار المهمة منتهية.
2. **ممنوع تحميل الخطوط عبر `@import url(...)` في CSS نهائيًا.** استخدم فقط `next/font/google` كما
   هو موضّح أعلاه. احذف أي سطر `@import` من `globals.css` الخاص بـ Google Fonts.
3. **ثبّت `lucide-react` فعليًا** (`npm install lucide-react`) واستبدل كل إيموجي مستخدم كأيقونة
   (🚨 ⚠️ ✅ إلخ) بمكوّن أيقونة حقيقي منها. ابحث بـ `grep -rn "🚨\|⚠️\|✅" src` وتأكد من نتيجة صفر.
4. **احذف مجلد `asstes/`** بالكامل من جذر المشروع (اسمه خطأ إملائي لـ assets، ومحتواه مكرَّر مع
   `public/logo.svg` وغير مستخدم في أي مكان — تحقق بـ `grep -rn "asstes" src` قبل الحذف للتأكد).
5. **أنشئ `src/components/ui/`** يحتوي على الأقل: `Button.tsx`, `Card.tsx`, `Badge.tsx`,
   `Table.tsx` — وأعد كتابة كل الجداول المتفرقة الحالية (`doctors-finance-table.tsx`,
   `users-table.tsx`, `payment-row.tsx`, وغيرها) لتستخدم هذه المكوّنات المشتركة بدل تكرار نفس
   الـ markup بأنماط مختلفة قليلاً في كل ملف.
6. بعد التنفيذ، قدِّم في ملخص العمل عدد الملفات المعدَّلة ونتيجة كل أمر `grep` أعلاه (يجب أن تكون صفر)
   كإثبات أن التعليمات طُبِّقت فعليًا وليس وصفيًا فقط.



## ٧. آليات الحماية — إلزامية على كل كود جديد

- **تشفير الحقول الحساسة**: الرقم القومي وأي حقل مشابه يُشفَّر عبر AES-256-GCM
  (`src/lib/crypto.ts`) قبل التخزين، أبدًا كنص صريح.
- **RBAC على مستويين**: Middleware (`src/middleware.ts`) لحماية المسارات، + فحص صريح داخل كل API
  route (`can()` من `src/lib/rbac.ts`) قبل تنفيذ أي عملية. لا تعتمد على الـ Middleware وحده.
- **تحقق صارم من المدخلات**: كل API route يستخدم zod schema قبل لمس قاعدة البيانات؛ أي endpoint
  جديد يجب أن يتبع نفس النمط.
- **رفع الملفات**: فحص نوع الملف (PDF/صور/Word فقط) وحجمه (حد أقصى 15MB) قبل الحفظ — موجود في
  `src/app/api/cases/[id]/attachments/route.ts`، لا تُزِل هذا الفحص.
- **سجل تدقيق شامل (Audit Log)**: كل إنشاء/تعديل على القضايا والإجراءات والقرارات والمدفوعات
  والمرفقات يُسجَّل تلقائيًا (من قام بالعملية، الوقت، القيمة قبل/بعد).
- **كلمات المرور**: bcrypt فقط (12 rounds كحد أدنى)، لا تخزين نص صريح تحت أي ظرف.
- عند إضافة أي endpoint جديد، تحقق من: (1) الجلسة موجودة، (2) الدور مصرَّح له بالعملية عبر `can()`،
  (3) نطاق البيانات مقيَّد بدور المستخدم (مثال: اللجنة الفرعية لا ترى إلا قضاياها)، (4) تسجيل Audit Log.

## ٨. حالة الإنجاز الحالية — ابدأ من هنا

تنبيه: هذا تحديث لاحق على نسخة كانت تعمل فعليًا محليًا (تسجيل دخول، قاعدة بيانات، شاشات كل الأدوار،
الصرف المالي التلقائي، إدارة المستخدمين، تغيير كلمة المرور)، وتم بالفعل تنفيذ فصل دور المتابعة
والتوجيه وربط التخصصات باللجان الفرعية. المطلوب الآن أولوية عاجلة لإصلاحات الخطوط والتناسق البصري
(القسم ٦)، ثم استكمال الباقي. أولوية التنفيذ:

0. **[عاجل]** إصلاح تحميل الخطوط (Cairo/Tajawal عبر `next/font/google`) وتطبيق مقياس الأحجام
   والتباعد الثابت في القسم ٦ على كل الشاشات — هذا ما أبلغ عنه المستخدم صراحة كمشكلة حالية
   ("الخطوط سيئة ولا يوجد تناسب في حجم الخطوط"). ابدأ من `layout.tsx` وبطاقات KPI في لوحة
   المتابعة والتوجيه لأنها أول ما يُشاهَد.
1. **فصل صلاحية التوجيه** من `REGISTRATION_CLERK` إلى دور جديد `ROUTING_OFFICER` — **تم تنفيذه**،
   تحقق فقط من عدم وجود مسارات قديمة متبقية تسمح للتسجيل بالتوجيه.
2. **شاشة المتابعة الزمنية لـ ROUTING_OFFICER** — **تم تنفيذها**، تحقق من أن حالة "0" تُعرض بلون
   محايد وليس تحذيريًا (انظر القسم ٦).
3. **`Specialty` ككيان منفصل** — **تم تنفيذه** ("سجل التخصصات الطبية" ظاهر في القائمة الجانبية)،
   تحقق أن واجهة الأدمن لإضافة لجنة فرعية جديدة وتخصصاتها تعمل فعليًا وليست عرضًا فقط.
4. **إعادة تصميم كل الشاشات** وفق هوية التصميم — **جزئي**: الشريط الجانبي لسه بلون أزرق كلاسيكي
   بدل `slate-900`/`teal-600` المطلوبين في القسم ٦؛ تأكد من تطبيقه على كل الصفحات وليس بعضها.

بعد إنجاز ما سبق، راجع `README.md` للنقاط المتبقية الأقدم (اختبارات آلية، Rate limiting فعلي
على مستوى الـ API، إلخ) وحدِّثه ليعكس الوضع الجديد.

## ٩. قواعد عامة للـ Agent

قبل أي تعديل تصميمي جديد، نفّذ أولاً **تدقيقًا شاملاً** على المستودع كاملاً (وليس فقط الملفات
المذكورة أعلاه كأمثلة) وأصلح كل ما يلي:

- **كل زر في كل شاشة** (بما فيها زر "تسجيل الخروج" في الشريط الجانبي) يجب أن يتبع حرفيًا أنماط
  الأزرار الثلاثة في القسم ٦ (أساسي/ثانوي/تحذيري) — لا زر بلون أو حجم أو حواف مختلفة عن النمط
  الموحّد. ابحث عن كل عنصر `<button>` و`<a>` بشكل زر في المشروع وطابقه يدويًا.
- **ملفات مكرَّرة أو orphan**: أي ملف غير مستورَد من أي مكان (component غير مستخدم، نسخة قديمة من
  شاشة تم استبدالها، أصول مكرَّرة زي حالة `asstes/` سابقًا) — احذفه، ولا تتركه "احتياطيًا".
- **أكواد متكررة (Duplication)**: أي منطق أو JSX متطابق أو شبه متطابق مكرَّر في أكثر من ملف (مثال:
  شكل الجدول، شكل البطاقة، منطق تنسيق التاريخ) يُستخرَج في مكوّن أو دالة مشتركة واحدة (`src/lib/`
  أو `src/components/ui/`) ويُستدعى من كل الأماكن، لا يُعاد كتابته في كل شاشة.
- **قدِّم في نهاية العمل قائمة**: (أ) كل ملف تم حذفه ولماذا، (ب) كل مكوّن مشترك جديد تم إنشاؤه وأي
  الملفات صارت تستخدمه، (ج) نتيجة أوامر التحقق (`grep`) المذكورة في القسم ٦. بدون هذه القائمة لا
  يُعتبر التدقيق مكتملًا.

قواعد إضافية:
- لا تغيّر أسماء الحقول أو الكيانات في `prisma/schema.prisma` دون سبب قوي — أي تعديل بنيوي يتطلب
  `prisma migrate dev` جديد ويجب أن يُذكر بوضوح في ملخص العمل.
- لا تُدخل أي اعتماد على خدمة سحابية خارجية (مصادقة، تخزين، قاعدة بيانات) — هذا قيد تصميمي أساسي وليس تفضيلًا.
- كل نص في الواجهة عربي RTL، وكل تعليق/توثيق في الكود عربي أو ثنائي اللغة كما هو متبع حاليًا في الملفات.
- عند الشك في قرار غير مذكور هنا، اسأل المستخدم بدلًا من الافتراض.
